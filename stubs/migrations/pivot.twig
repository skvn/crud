<?php

use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class {{pivot.class}} extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        if (!Schema::hasTable('{{pivot.table_name}}')) {
            Schema::create('{{pivot.table_name}}', function (Blueprint $table) {
            $table->integer('{{pivot.self_key}}')->unsigned()->index();
            $table->integer('{{pivot.foreign_key}}')->unsigned()->index();
            $table->primary(['{{pivot.self_key}}', '{{pivot.foreign_key}}']);
            });
        }


    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::drop('{{pivot.table_name}}');
    }
}
