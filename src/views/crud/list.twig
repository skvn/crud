{% import "crud::widgets/macro.twig" as widgets%}

{{ widgets.filters(crudObj) }}

<div class="row">
    <div class="col-lg-12">
        {%  if crudObj.isTree()%}
            {%  if crudObj.getListConfig('type') == 'dt_tree' %}
                {{ widgets.table(crudObj) }}
            {% else  %}
                {{ widgets.tree(crudObj) }}
            {% endif %}

        {%  else  %}
            {{ widgets.table(crudObj) }}
        {% endif %}
    </div>
</div>