{% import "crud::widgets/macro.twig" as widgets%}

{{ widgets.filters(crudObj) }}

<div class="row">
    <div class="col-lg-12">
        {%  if crudObj.isTree()%}
            {%  if crudList.getParam('list_type') == 'dt_tree' or crudList.getParam('list_type') == 'dt_flat' %}
                {{ widgets.table(crudList) }}
            {% else  %}
                {{ widgets.tree(crudObj) }}
            {% endif %}

        {%  else  %}
            {{ widgets.table(crudList) }}
        {% endif %}
    </div>
</div>