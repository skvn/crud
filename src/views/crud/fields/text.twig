{% include 'crud::crud/fields/label.twig' %}
<input {{ input.config.extra|raw }} class="form-control"
        {{ input.config.required ? 'required' }}    name="{{ input.name }}"
       type="text"
       value="{{ input.getValue() }}"
       data-default="{{ input.config.default }}"
        />
