{% if input.config.editor %}
<div style="height: 570px;">
{% endif %}
<textarea {{ input.config.rows ? ('rows="'~input.config.rows~'"')|raw }} class="form-control {{ input.config.editor ? 'html_editor' }}"    {{ input.config.required ? 'required' }} name="{{ input.name }}"  data-default="{{ input.getDefaultTo() }}" >{{ input.getValue()|raw}}</textarea>
{% if input.config.editor %}
</div>
{% endif %}