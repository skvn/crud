<input  id="{{ input.getUniqueId() }}" name="__{{ input.name }}" data-model="{{ input.config.model }}" class="form-control tagsinput"   />
<input type="hidden" id="{{ input.getUniqueId() }}_hidden" name="{{ input.name }}" value="{{ input.getValue()|join(',') }}" />

