{% if input.config.acl|allowed %}
    {%  if input.config.type == 'multi_file' %}
        {% include input.controlTemplate() %}
    {%  else  %}
    <div class="row" data-ref="{{ input.name }}">
        <div class="col-md-{{ COL_WIDTH?COL_WIDTH:12 }}" style="margin-top: 10px;">
           {% include input.controlTemplate() %}
        </div>
    </div>

    {% endif %}
{% endif %}