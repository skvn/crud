{% set used_fields = used_fields|merge([f]) %}
<span style="cursor: move"  data-rel="{{ f }}" class="list-group-item list-group-item-info"><b>{{ f }}</b> {{ fld.relation ? 'Relation ' }}{{ fld.title }} ({{ fld.type }})</span>